{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "master",
  "password": "masterkey",
  "database": "express",
  "entities": ["src/db/entities/*.entity.ts"],
  "migrations": ["src/db/migrations/*.ts"],
  "cli": {
    "migrationsDir": "src/db/migrations"
  }
}
